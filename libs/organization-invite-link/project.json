{
  "name": "organization-invite-link",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/organization-invite-link/src",
  "projectType": "library",
  "tags": [],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/libs/organization-invite-link",
        "main": "libs/organization-invite-link/src/index.ts",
        "tsConfig": "libs/organization-invite-link/tsconfig.lib.json",
        "assets": ["libs/organization-invite-link/*.md"],
        "rootDir": "libs/organization-invite-link/src"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["libs/organization-invite-link/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "libs/organization-invite-link/jest.config.js"
      }
    }
  }
}
